name: CI-CD Deploy

on:
  push:
    branches:
      - deploy

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build Ventas
        run: docker build -t jvc1016/ventas-service:latest ./back-Ventas_SpringBoot/Springboot-API-REST

      - name: Push Ventas
        run: docker push jvc1016/ventas-service:latest

      - name: Build Despachos
        run: docker build -t jvc1016/despachos-service:latest ./back-Despachos_SpringBoot/Springboot-API-REST-DESPACHO

      - name: Push Despachos
        run: docker push jvc1016/despachos-service:latest

      - name: Build Frontend
        run: docker build -t jvc1016/front-despacho:latest ./front_despacho

      - name: Push Frontend
        run: docker push jvc1016/front-despacho:latest
